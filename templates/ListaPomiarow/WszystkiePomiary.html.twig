{% extends 'base.html.twig' %}

{% block title %}Pomiary{% endblock %}

{% block cont %}

    <div class="container" >
        <div class="col-12 justify-content-center text-center">
        <div class="btn-group ">

            <a href="{{ path('Wszystkie_pomiary_rok',{'rok':current_Year-3}) }}" class="btn btn-outline-danger">{{ current_Year-3 }}</a>
            <a href="{{ path('Wszystkie_pomiary_rok',{'rok':current_Year-2}) }}" class="btn btn-outline-danger">{{ current_Year-2 }}</a>
            <a href="{{ path('Wszystkie_pomiary_rok',{'rok':current_Year-1}) }}" class="btn btn-outline-danger">{{ current_Year-1 }}</a>
            <a class="btn btn-danger">{{ current_Year }}</a>
            <a href="{{ path('Wszystkie_pomiary_rok',{'rok':current_Year+1}) }}" class="btn btn-outline-danger">{{  current_Year+1 }}</a>
            <a href="{{ path('Wszystkie_pomiary_rok',{'rok':current_Year+2}) }}" class="btn btn-outline-danger">{{ current_Year+2 }}</a>
            <a href="{{ path('Wszystkie_pomiary_rok',{'rok':current_Year+3}) }}" class="btn btn-outline-danger">{{ current_Year+3 }}</a>
        </div>
        </div>
    </div>
  {% include 'ListaPomiarow/ListaPomiarow.html.twig' %}

{% endblock %}
